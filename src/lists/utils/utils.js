const countries = require('./countries')
const cldFileAdapter = require('./image_adaptor')
const access = require('./access')
const keys = require('./keys')


module.exports = {
  countries: countries,
  cldFileAdapter: cldFileAdapter,
  access :access,
  keys:keys
}